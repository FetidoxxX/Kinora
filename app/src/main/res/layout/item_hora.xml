<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android/tools"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:layout_marginEnd="8dp">
    <ImageView
        android:id="@+id/horaFondo"
        android:layout_width="64dp"
        android:layout_height="28dp"
        android:layout_marginStart="5dp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"  app:srcCompat="@drawable/hora"
        />

    <TextView
        android:id="@+id/textView15"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="05:27"
        android:textColor="#FFFFFF"
        android:textSize="12sp"
        app:layout_constraintBottom_toBottomOf="@+id/horaFondo"
        app:layout_constraintEnd_toEndOf="@+id/horaFondo"
        app:layout_constraintStart_toStartOf="@+id/horaFondo"
        app:layout_constraintTop_toTopOf="@+id/horaFondo" />
</androidx.constraintlayout.widget.ConstraintLayout>